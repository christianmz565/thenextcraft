# Repository Agent Guidelines

## Self-Hosted Architecture & Rules

This project is **strictly self-hosted** using Docker Compose. All development, testing, and deployment workflows MUST be designed around self-hosting.

- **Infrastructure Stack**:
  - Self-hosted Convex backend (`ghcr.io/get-convex/convex-backend`)
  - Self-hosted Convex dashboard (`ghcr.io/get-convex/convex-dashboard`)
  - PostgreSQL 16 database
  - MinIO object storage (S3 compatible)
- **Environment**: All local dev variables are supplied by `.env.dev` (gitignored). Keep `docker-compose.yml` as reference only and use `docker-compose.dev.yml` for dev environment.
 - **Convex Tooling**:
   - Do NOT run cloud Convex commands targeting Convex Cloud (`convex dev --cloud` etc).
   - Self-hosted Convex backend stores function environment variables in its deployment store. Set `SITE_URL`, `AUTH_GOOGLE_ID`, and `AUTH_GOOGLE_SECRET` via `bun node_modules/.bin/convex env set <KEY> <VALUE> --url http://127.0.0.1:3210 --admin-key <admin-key>` (container OS environment variables are not injected into V8 isolates).
   - Frontend connects via `NEXT_PUBLIC_CONVEX_URL`, `CONVEX_SELF_HOSTED_URL`, and `CONVEX_SELF_HOSTED_ADMIN_KEY`.
- **Package Manager**: Use `bun` exclusively (`bun install`, `bun run ...`). Never use `npm`.

---

## Environment Files

Canonical files only — duplicates (`/.env`, `/.env.local`, `packages/backend/.env.local` auto-generated) were removed.

| Path | Tracked | Loaded by | Purpose |
|---|---|---|---|
| `.env.dev` | No (gitignored) | `docker compose --env-file .env.dev -f docker-compose.dev.yml` + `bun --env-file=.env.dev run --filter <workspace> dev` | **Single source of truth for local self-hosted dev.** Contains: `GOOGLE_CLIENT`/`GOOGLE_SECRET` (mapped to `AUTH_GOOGLE_ID`/`SECRET` in container), `PORT=3000` (Next.js), `CONVEX_PORT=3210`/`SITE_PROXY_PORT=3211`/`DASHBOARD_PORT=6791`, `SITE_URL`, `NEXT_PUBLIC_CONVEX_URL`/`CONVEX_SELF_HOSTED_URL`/`CONVEX_CLOUD_ORIGIN`/`CONVEX_SITE_ORIGIN`/`NEXT_PUBLIC_DEPLOYMENT_URL`, `POSTGRES_*`/`POSTGRES_URL`/`DO_NOT_REQUIRE_SSL`, `MINIO_*`/`S3_*`/`AWS_*` (`AWS_S3_FORCE_PATH_STYLE=true`, `AWS_S3_DISABLE_SSE=true`, `AWS_S3_DISABLE_CHECKSUMS=true` required for MinIO), and `CONVEX_SELF_HOSTED_ADMIN_KEY` (appended by `generate-admin-key.sh`). |
| `.env.example` | Yes | — (template) | Root template documenting cloud (`CONVEX_DEPLOYMENT`, `CONVEX_URL`, `NEXT_PUBLIC_CONVEX_URL` cloud URL, `AUTH_GOOGLE_ID/SECRET`, `SITE_URL`) vs self-hosted (`CONVEX_SELF_HOSTED_URL/ADMIN_KEY`, container-env manual per `labs.convex.dev/auth/setup/manual`). Copy to `.env.dev`/`.env.local` as needed. |
| `apps/web/.env.example` | Yes | — (template) | Frontend template: `NEXT_PUBLIC_CONVEX_URL` cloud vs self-hosted (`NEXT_PUBLIC_CONVEX_URL=http://127.0.0.1:3210`, `CONVEX_SELF_HOSTED_URL`, `CONVEX_SELF_HOSTED_ADMIN_KEY`), `SITE_URL`, OAuth redirect note (`CONVEX_SITE_ORIGIN` → `http://127.0.0.1:3211/api/auth/callback/google` self-hosted vs `*.convex.site` cloud). |
| `apps/web/.env.local` | No (gitignored) | Next.js (`next dev`/`next build` auto-loads) + `ConvexClientProvider` (`process.env.NEXT_PUBLIC_CONVEX_URL`) | **Frontend runtime.** Created via `bun run convex:admin-key` (writes `CONVEX_SELF_HOSTED_ADMIN_KEY`) plus `SITE_URL`. Contains `NEXT_PUBLIC_CONVEX_URL`, `CONVEX_SELF_HOSTED_URL`, `CONVEX_SELF_HOSTED_ADMIN_KEY`, `SITE_URL`, and duplicated `AUTH_GOOGLE_*` (mirrored from `.env.dev`; `apps/web/env.ts` only validates `NEXT_PUBLIC_CONVEX_URL` but provider needs URL). Used by `ConvexAuthNextjsProvider` + `ConvexAuthNextjsServerProvider`. |
| `packages/backend/.env.example` | Yes | — (template) | Backend cloud template: `CONVEX_DEPLOYMENT`/`CONVEX_URL`, `SITE_URL`, `AUTH_GOOGLE_ID/SECRET` with `bunx --cwd packages/backend convex env set ...` instructions. **Self-hosted:** ignored — set `SITE_URL`/`AUTH_GOOGLE_*` via `.env.dev` → `docker-compose.dev.yml` container env (CLI `convex env set` unsupported, see `packages/backend/README.md` + `https://labs.convex.dev/auth/setup/manual`). |
| `packages/backend/.env.local` | No (gitignored, auto-generated, currently removed) | `convex dev`/`convex codegen` | Generated by `bun --env-file=.env.dev run --filter backend dev` / `codegen` containing `CONVEX_URL=http://127.0.0.1:3210` + `CONVEX_SITE_URL=http://127.0.0.1:3211`. Points CLI to self-hosted deployment; regenerated on next `convex:dev`. Not committed. |
| `packages/backend/convex/env.ts` + `apps/web/env.ts` | Yes | Runtime validation (`@t3-oss/env-*`, `zod`) | `apps/web/env.ts` validates `NEXT_PUBLIC_CONVEX_URL` (optional url) + `NODE_ENV`; `packages/backend/convex/env.ts` validates `CONVEX_DEPLOYMENT`/`CONVEX_URL` (cloud, optional). Self-hosted vars (`CONVEX_SELF_HOSTED_*`, `AWS_S3_*`, `SITE_URL`) bypass validation via `skipValidation` / container env. |

Removed: `/.env` (stale duplicate of `.env.dev` with wrong `PORT=3210`, missing S3/PG vars, never referenced), `/.env.local` (stale root duplicate of `apps/web/.env.local` with mismatched admin key, Next.js at root has no runtime), `packages/backend/.env.local` (auto-generated, now cleaned; will be recreated by `convex:dev`).

---

## Launch Guide (Local Development Setup)

### 1. Start Dev Infrastructure Stack
Run Docker Compose using `.env.dev` to launch PostgreSQL, MinIO, bucket creation, Convex backend, and Convex dashboard:

```bash
docker compose --env-file .env.dev -f docker-compose.dev.yml up -d
```

### 2. Check Container Health & Status
Verify that all services are running and healthy:

```bash
docker compose --env-file .env.dev -f docker-compose.dev.yml ps
```

### 3. Generate Convex Admin Key
Generate the self-hosted admin key required by the frontend:

```bash
bun run convex:admin-key
# or directly: ./scripts/generate-admin-key.sh
```

### 4. Configure Frontend Environment (`apps/web/.env.local`)
Create or update `apps/web/.env.local` with the generated admin key:

```env
NEXT_PUBLIC_CONVEX_URL="http://127.0.0.1:3210"
CONVEX_SELF_HOSTED_URL="http://127.0.0.1:3210"
CONVEX_SELF_HOSTED_ADMIN_KEY="<admin-key-from-step-3>"
SITE_URL="http://localhost:3000"
```

### 5. Start the Web Frontend
Run the Next.js development server:

```bash
bun run dev
```

### 6. Stop or Restart Stack
- **Stop stack**: `docker compose --env-file .env.dev -f docker-compose.dev.yml stop`
- **Tear down containers**: `docker compose --env-file .env.dev -f docker-compose.dev.yml down`
- **View logs**: `docker compose --env-file .env.dev -f docker-compose.dev.yml logs -f`

---

## Service Endpoints Reference

| Service | Local Endpoint | Notes |
|---|---|---|
 | **Web Frontend** | `http://localhost:3000` | Next.js app (`next dev`, via `bun run dev` → `concurrently` backend + web) |
| **Convex Backend HTTP** | `http://127.0.0.1:3210` | Client API endpoint (`CONVEX_SELF_HOSTED_URL`/`NEXT_PUBLIC_CONVEX_URL`) |
| **Convex Auth Site Origin** | `http://127.0.0.1:3211` | OAuth callback target (`CONVEX_SITE_ORIGIN` → `/api/auth/callback/google`) |
| **Convex Dashboard** | `http://localhost:6791` | Self-hosted Web UI (`NEXT_PUBLIC_DEPLOYMENT_URL`) |
| **MinIO API** | `http://localhost:9000` | S3 object storage (`S3_ENDPOINT_URL=http://minio:9000`, `AWS_S3_FORCE_PATH_STYLE=true`) |
| **MinIO Web Console** | `http://localhost:9001` | User: `minioadmin`, Pass: `minioadmin` |
| **PostgreSQL DB** | `localhost:5432` | User: `convex`, Pass: `convex`, DB: `convex_self_hosted` (`POSTGRES_URL`) |
