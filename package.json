{
  "name": "thenextcraft",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "check": "biome check .",
    "check:write": "biome check --write .",
    "check:ci": "biome ci .",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "lint:write": "biome lint --write .",
    "convex:dev": "bun --env-file=.env.dev --filter backend convex dev",
    "convex:codegen": "bun --env-file=.env.dev --filter backend convex codegen",
    "convex:admin-key": "./scripts/generate-admin-key.sh",
    "dev:web": "bun --env-file=.env.dev --filter web dev",
    "dev": "bun --env-file=.env.dev --filter web dev"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.11",
    "@types/node": "^26.4.0",
    "typescript": "5.9.2"
  },
  "packageManager": "bun@1.3.13",
  "engines": {
    "bun": ">=1.1.0"
  }
}
